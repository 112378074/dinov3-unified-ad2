"""Unified-model evaluator skeleton: single forward -> head_D (binary+score), head_R+head_D fused
float (LEARNED gate replaces hand-tuned pen/lam), 4 metrics via the sibling package's
ad2_pipeline_eval. Implemented at E3."""
raise SystemExit('implemented at E3 — see docs/PROPOSAL.md')
